  <%= form_for(@travel) do |f| %><%#- should not Client.new but @client need to be double-check in the controller%>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :firstname, placeholder: "Test..." %>
  </div>
  <%= f.submit "Add", class: "btn btn-primary" %>
<% end %>

 <% if @bankaccount.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      The form contains <%= pluralize(@bankaccount.errors.count, "error") %>.
    </div>
    <ul>
    <% @bankaccount.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

 <% if @bankaccount.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      Le formulaire contient <%= pluralize(@bankaccount.errors.count, "erreur") %>.
    </div>
    <ul>
    <% @bankaccount.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

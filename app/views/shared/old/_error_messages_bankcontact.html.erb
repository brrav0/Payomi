 <% if @bankcontact.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      Le formulaire contient <%= pluralize(@bankcontact.errors.count, "erreur") %>.
    </div>
    <ul>
    <% @bankcontact.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

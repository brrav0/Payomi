 <% if @clientcontact.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      The form contains <%= pluralize(@clientcontact.errors.count, "error") %>.
    </div>
    <ul>
    <% @clientcontact.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

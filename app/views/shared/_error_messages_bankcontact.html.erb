 <% if @bankcontact.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      The form contains <%= pluralize(@bankcontact.errors.count, "error") %>.
    </div>
    <ul>
    <% @bankcontact.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

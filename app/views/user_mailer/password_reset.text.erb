<%#- it seems that using French-specific characters change the encoding from 7Bit to quoted-printable so do not use them %>
Initialisation du mot de passe :

<%= edit_password_reset_url(@user.reset_token, email: @user.email) %>

Ce lien de ne sera plus valable dans deux heures.



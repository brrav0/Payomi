<script>
  $(function () { 
    $("[data-toggle='tooltip']").tooltip(); 
});
</script>


	  <div class="row">
            <div class="col-md-4">
		  <h2>Détail de votre circularisation</h2>
		  <hr>
            </div>
            <div class="col-md-offset-5 col-md-3">
              <ul class="nav nav-tabs">
    <li class="<%= is_active? bankaccounts_path %>">
      <%= link_to "Revenir aux circularisations", confirmations_path %>
    </li>
  </ul>

            </div>
          </div>

		<div class="row">
			<div class="col-md-6">
			<h4>Information de votre client</h4>
                        Nom : <%= @client.name %><br>
                        Numéro et rue : <%= @client.number_and_street %><br>
                        Code Postal : <%= @client.zip_code %><br>
                        Ville : <%= @client.city %><br><br>
    			Signataire autorisé : <%= @client.email %>
			</div>

			<div class="col-md-6">
			    <h4>Vos informations</h4><br>
			    Nom : <%= @cac.name %><br>
                            Email : <%= @cac.email %><br>
			</div>
		</div>
                <hr>

                <div class="row">
                  <div class="col-md-12">
		            <h4>Circularisation</h4>
			    Référence  de la circularisation: <%= @confirmation.reference %><br><br>
Add here the attached file (mandatory as a confirmation can only be sent if all field have been filled out) - if the status is "sent" the answer from the bank is laid out as pending. If the status is "completed" the auditor should be able to retrieve the answer from the bank. 
<br>
				<hr>
				<%-# START: Here is the scan block -%>
				<div class="col-md-6">
				<% if !flash[:notice].blank? %>
				  <div class="alert alert-info">
					<%= flash[:notice] %>
				  </div>
				<% end %>

				<h4>Scan relatif à la demande</h4>
				<table class="table table-bordered table-striped">
				  <thead>
					<tr>
					  <th>Scan</th>
					  <th>Lien téléchargement</th>
					</tr>
				  </thead>
				  <tbody>
					<% @attachedfiles.each do |attachedfile| %>
					  <tr>
						<td><%= attachedfile.name %></td>
						<td><%= link_to "Cliquez pour télécharger le scan de la demande", attachedfile.attachment_url, class: "btn btn-primary" %></td>
					  </tr>
					<% end %>
				  </tbody>
				</table>
		
				</div>
				
				<%-# END: Here is the scan block -%>


                 </div>
                 
                 <div class="col-md-9">
                 <br><br><br><br>
                  <%= form_for (@confirmation) do |f| %>

                  <%= f.submit "Confirmer",:name=>"conf", class: "btn btn-lg btn-primary btn-confirm" %>
                  <% end %>
 				</div>


                  
                </div>



                <hr>
		 <div class="row">
                  <div class="col-md-12">
		            <h4>Suivi d'activité</h4>
		            <table class="table table-condensed">
 				<tr>
					<th>Description</th>
					<th>Email</th>
					<th>Date et heure</th>
				</tr>
				<tr>
					<td>Envoi</td>
					<td><%= @cac.email %></td>
					<td><%= @confirmation.created_at %></td>
				</tr>
				<tr>
					<td>Réponse</td>
                                        <% if @confirmation.status == "Envoyée" %>
					<td>En attente de votre réponse</td>
					<td>En attente de votre réponse</td>
                                        <% else %>
                                        <td>La banque a répondu</td>
                                        <td>La banque a répondu</td>
                                    
                                        <% end %>
				</tr>
                            </table>
                  </div>

                </div>

